# JSON field paths for modcluster, checked by JsonSchemaContractTest.
# Regenerate: mvn test -Dtest=JsonSchemaContractTest -Djws.schema.update=true
schemaVersion=1.0
count
listeners
listeners[].advertise
listeners[].advertiseGroupAddress
listeners[].advertisePort
listeners[].balancer
listeners[].connector
listeners[].extraAttributes
listeners[].listenerClassName
listeners[].proxyList
listeners[].stickySession
listeners[].stickySessionCookie
schemaVersion
