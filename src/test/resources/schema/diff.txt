# JSON field paths for diff, checked by JsonSchemaContractTest.
# Regenerate: mvn test -Dtest=JsonSchemaContractTest -Djws.schema.update=true
schemaVersion=1.0
changeCount
changes
changes[].left
changes[].path
changes[].right
changes[].type
left
right
schemaVersion
